\input{preamble}

\pagestyle{empty}

\begin{document}
<% 
    files=Dir.glob("nmat[0-9][0-9][a-d].tex") 
    files=files.sort {|f,g| File.mtime(f)<=>File.mtime(g)}
%>
\input{<%=File.basename(files.last,".tex")%>}

\end{document}
